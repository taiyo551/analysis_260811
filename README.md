# analysis_260811
Analyzing the impact of input token length on retrieval accuracy in dense retrieval models

密検索モデルにおいて，入力トークン長が検索精度に与える影響を分析した．

### 仮想環境の構築と有効化及びpipの更新
```
conda create -n ir_datasets python=3.12 -y
conda activate ir_datasets
python -m pip install --upgrade pip
```

### 必要なパッケージのインストール
```
python -m pip install -r requirements.txt
```
